# Backend architecture

Backend scaffolding follows the incident-agent architecture: FastAPI app layers, domain models and schemas, services, tools, agent graph nodes, LLM/RAG modules, workers, webhooks, notifications, migrations, and tests.

All folders are intentionally scaffold-only. No runtime backend or integrations are enabled in the frontend prototype yet.
